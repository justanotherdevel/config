import * as glob from "glob";
export declare function all(pattern: string, cmd: string, opts?: glob.IOptions): Promise<string>;
export declare function foreach(pattern: string, cmd: string, opts?: glob.IOptions): Promise<string[]>;
