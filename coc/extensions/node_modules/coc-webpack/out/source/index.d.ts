import { IList, ListAction, ListContext, ListItem } from 'coc.nvim';
export default class WebpackList implements IList {
    readonly name = "webpack";
    readonly description = "webpack config options";
    readonly defaultAction = "default";
    actions: ListAction[];
    constructor();
    loadItems(_context: ListContext): Promise<ListItem[]>;
}
