import { ListContext, ListItem, BasicList, Neovim } from 'coc.nvim';
export default class WebpackErrorList extends BasicList {
    readonly name = "webpackErrors";
    readonly description = "webpack build error list";
    readonly defaultAction = "open";
    constructor(nvim: Neovim);
    loadItems(_context: ListContext): Promise<ListItem[]>;
}
